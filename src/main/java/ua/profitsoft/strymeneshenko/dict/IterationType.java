package ua.profitsoft.strymeneshenko.dict;

//We use to limit the set of valid values for the type of list crawl options.
public enum IterationType {
    BASIC_LOOP,
    FOREACH,
    BASIC_LOOP_WITH_ITERATOR,
    ITERATOR_WITH_WHILE_LOOP,
    LAMBDA_EXPRESSION
}
